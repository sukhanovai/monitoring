# Core dependencies
python-telegram-bot>=20.0
paramiko>=3.0
psutil>=5.9
# Database
sqlite3
# Web interface (optional)
flask>=2.0
# Windows monitoring (optional)
pywinrm>=0.4
wmi>=1.5
# Logging and utilities
colorlog>=6.7  # Для цветного логирования (опционально)
