# 🤖 Telegram-бот мониторинга серверов

**Server Monitoring System** — это Telegram-бот и набор сервисов для контроля состояния серверов, бэкапов и инфраструктуры. Проект поддерживает модульную архитектуру, расширения, CLI-проверки и веб-интерфейс.

## 🧩 Функционал

### Основные возможности
- ✅ Управление мониторингом через Telegram и веб-интерфейс.
- ✅ Кнопочное меню, быстрые команды и контекстные меню по серверам.
- ✅ Уведомления о недоступности и критических событиях.
- ✅ Гибкая настройка порогов и расписаний.
- ✅ Точечные проверки одного сервера (доступность/ресурсы).

### Проверки и мониторинг
- Доступность: **Ping / SSH / RDP / порты**.
- Ресурсы: **CPU / RAM / Disk**, а также **load average** и **uptime** (если доступны).
- Отдельные проверки ресурсов: CPU/RAM/Disk.
- Автоматические алерты по порогам ресурсов.

### Отчёты
- Утренний отчёт по доступности (автоматический и ручной `/report`).
- Статистика работы мониторинга.

### Бэкапы и инфраструктура
- Мониторинг бэкапов **Proxmox**.
- Мониторинг бэкапов **БД**.
- Мониторинг **ZFS** по почтовым уведомлениям.
- Сбор данных из почтовых уведомлений.

### Управление и администрирование
- Включение/отключение расширений из Telegram.
- Управление настройками прямо в боте.
- «Тихий режим» для алертов.
- Диагностика SSH/Windows.

## 🧠 Архитектура

- **core/** — ядро мониторинга и роутер задач.
- **bot/** — Telegram-бот, команды и меню.
- **modules/** — фоновые модули (ресурсы, отчёты, точечные проверки, почта).
- **extensions/** — расширения (бэкапы, веб-интерфейс, проверки серверов).
- **config/** — конфигурация и значения по умолчанию.

## 🚀 Запуск на чистой системе (Ubuntu/Debian)

### 1. Установите системные зависимости
```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip git
```

### 2. Клонируйте репозиторий
```bash
git clone https://github.com/sukhanovai/monitoring.git
cd monitoring
```

### 3. Создайте виртуальное окружение и установите зависимости
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4. Настройте конфигурацию
Конфигурация хранится в базе `data/settings.db` и загружается через
`config/db_settings.py`. Если в БД нет значений, используются значения по
умолчанию из `config/settings.py`.

Для первого запуска на чистой системе проще всего задать минимум в
`config/settings.py`:
```bash
nano config/settings.py
```

Минимально необходимые параметры:
```python
TELEGRAM_TOKEN = "YOUR_BOT_TOKEN"
CHAT_IDS = ["123456789"]
```

После первого запуска база `data/settings.db` создаётся автоматически и при
наличии значений в ней они будут иметь приоритет над `config/settings.py`.

### 5. Создайте Telegram-бота
1. Напишите @BotFather в Telegram.
2. Выполните `/newbot`.
3. Укажите имя и username бота.
4. Скопируйте выданный токен.

### 6. Получите Chat ID
Отправьте любое сообщение боту и выполните:
```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates"
```
Найдите поле:
```json
"chat": {"id": 123456789}
```

### 7. Запуск проекта
Основной мониторинг:
```bash
python main.py
```

Мониторинг почты (если используется):
```bash
python -m modules.improved_mail_monitor
```

## 🧪 Проверки и диагностика

Проверка конфигурации:
```bash
python -c "import config; print('✅ Конфигурация загружена')"
```

Проверка зависимостей:
```bash
python -c "import flask, telegram, paramiko; print('✅ Зависимости OK')"
```

## 🧪 CLI-режим для проверок

Поддерживаются быстрые проверки без запуска бота:
```bash
python main.py --check availability
python main.py --check resources
python main.py --check targeted_checks --server 192.168.1.10 --mode resources
```

Дополнительные параметры:
- `--server` — IP или имя сервера для точечной проверки.
- `--mode` — тип точечной проверки: `availability` или `resources`.
- `--reload-servers` — перечитать список серверов перед проверкой.
- `--dry-run` — тестовый запуск без сетевых действий.

## 🤖 Команды Telegram-бота

Основные команды:
- `/start` — главное меню.
- `/check` — быстрая проверка серверов.
- `/status` — статус мониторинга.
- `/servers` — список серверов.
- `/report` — утренний отчёт (вручную).
- `/stats` — статистика.
- `/control` — управление мониторингом.
- `/silent` — тихий режим.
- `/extensions` — управление расширениями.
- `/settings` — управление настройками.
- `/check_server` — проверка одного сервера.
- `/check_res` — ресурсы одного сервера.

Команды бэкапов доступны при включенных расширениях:
- `/backup`, `/backup_search`, `/backup_help` — Proxmox.
- `/db_backups` — бэкапы БД.

## 🔧 Расширения

Расширения включаются через конфигурацию и меню бота.
Пример доступных расширений:
```python
AVAILABLE_EXTENSIONS = {
    "backup_monitor": {
        "name": "📊 Мониторинг бэкапов Proxmox",
        "enabled_by_default": True
    },
    "database_backup_monitor": {
        "name": "🗃️ Мониторинг бэкапов БД",
        "enabled_by_default": True
    },
    "zfs_monitor": {
        "name": "🧊 Мониторинг ZFS",
        "enabled_by_default": True
    },
    "resource_monitor": {
        "name": "💻 Мониторинг ресурсов",
        "enabled_by_default": True
    },
    "web_interface": {
        "name": "🌐 Веб-интерфейс",
        "enabled_by_default": True
    },
    "email_processor": {
        "name": "📧 Обработка почты",
        "enabled_by_default": True
    }
}
```

## 🌐 Веб-интерфейс
Если включено расширение `web_interface`, панель доступна по адресу:
```
http://<YOUR_IP>:5000
```
Из веб-интерфейса можно запускать проверку ресурсов и формирование утреннего отчёта.
Рекомендуется ограничивать доступ локальной сетью.

## ⚙️ Production-развёртывание (systemd)

Создайте unit-файл:
```bash
sudo nano /etc/systemd/system/server-monitor.service
```

Пример:
```ini
[Unit]
Description=Server Monitoring System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/monitoring
ExecStart=/opt/monitoring/venv/bin/python /opt/monitoring/main.py
ExecStartPost=/bin/sleep 10
ExecStartPost=/opt/monitoring/venv/bin/python /opt/monitoring/modules/improved_mail_monitor.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация:
```bash
sudo systemctl daemon-reload
sudo systemctl enable server-monitor
sudo systemctl start server-monitor
```

Проверка состояния:
```bash
systemctl status server-monitor
```

## 📄 Лицензия
MIT License — подробности в файле [LICENSE](LICENSE).

