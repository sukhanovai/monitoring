# 🤖 Telegram-бот мониторинга серверов

**Server Monitoring System** — это Telegram-бот и набор сервисов для контроля состояния серверов, бэкапов и инфраструктуры. Проект поддерживает модульную архитектуру, расширения и веб-интерфейс.

## 🧩 Функционал бота

### Основные возможности
- ✅ Управление мониторингом через Telegram.
- ✅ Меню с кнопками и удобные команды.
- ✅ Уведомления о недоступности серверов и критических событиях.
- ✅ Гибкая настройка порогов и расписаний.

### Проверки и мониторинг
- Доступность серверов: **SSH / RDP / Ping**.
- Ресурсы: **CPU / RAM / Disk**.
- История метрик и отчёты.

### Бэкапы и отчёты
- Мониторинг бэкапов **Proxmox**, **БД (1C/PostgreSQL/MySQL)** и файлов.
- Сбор данных из почтовых уведомлений.
- Утренние отчёты и ручной запрос статуса.

### Диагностика и управление
- Команды диагностики SSH/Windows.
- Режим «тихий час».
- Включение/выключение расширений из Telegram.

## 🧠 Архитектура

- **core/** — ядро мониторинга.
- **bot/** — Telegram-бот и меню.
- **modules/** — фоновые процессы (например, мониторинг почты).
- **extensions/** — расширения (веб-интерфейс, бэкапы и др.).
- **config/** — шаблоны конфигурации.

## 🚀 Запуск на чистой системе (Ubuntu/Debian)

Ниже — подробная пошаговая инструкция для чистого сервера.

### 1. Установите системные зависимости
```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip git
```

### 2. Клонируйте репозиторий
```bash
git clone https://github.com/sukhanovai/monitoring.git
cd monitoring
```

### 3. Создайте виртуальное окружение и установите зависимости
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4. Создайте конфигурацию
Скопируйте шаблон конфигурации:
```bash
cp config_template.py config.py
```
Или для минимального старта:
```bash
cp config_template_simple.py config.py
```

Откройте файл и задайте параметры:
```bash
nano config.py
```

Минимально необходимые параметры:
```python
TELEGRAM_TOKEN = "YOUR_BOT_TOKEN"
CHAT_IDS = ["123456789"]
```

### 5. Создайте Telegram-бота
1. Напишите @BotFather в Telegram.
2. Выполните `/newbot`.
3. Укажите имя и username бота.
4. Скопируйте выданный токен.

### 6. Получите Chat ID
Отправьте любое сообщение боту и выполните:
```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates"
```
Найдите поле:
```json
"chat": {"id": 123456789}
```

### 7. Запуск проекта
Основной мониторинг:
```bash
python main.py
```

Мониторинг почты (если используется):
```bash
python -m modules.improved_mail_monitor
```

### 8. Проверка
В Telegram отправьте:
```
/start
```
Если появилось меню — бот работает.

## 🧪 Проверки и диагностика

Проверка конфигурации:
```bash
python -c "import config; print('✅ Конфигурация загружена')"
```

Проверка зависимостей:
```bash
python -c "import flask, telegram, paramiko; print('✅ Зависимости OK')"
```

## 🔧 Расширения

Расширения включаются через конфигурацию и меню бота. Пример:
```python
AVAILABLE_EXTENSIONS = {
    "backup_monitor": {
        "name": "📊 Мониторинг бэкапов Proxmox",
        "enabled_by_default": True
    },
    "web_interface": {
        "name": "🌐 Веб-интерфейс",
        "enabled_by_default": True
    }
}
```

## 🌐 Веб-интерфейс
Если включено расширение `web_interface`, панель доступна по адресу:
```
http://<YOUR_IP>:5000
```
Рекомендуется ограничивать доступ локальной сетью.

## ⚙️ Production-развёртывание (systemd)

Создайте unit-файл:
```bash
sudo nano /etc/systemd/system/server-monitor.service
```

Пример:
```ini
[Unit]
Description=Server Monitoring System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/monitoring
ExecStart=/opt/monitoring/venv/bin/python /opt/monitoring/main.py
ExecStartPost=/bin/sleep 10
ExecStartPost=/opt/monitoring/venv/bin/python /opt/monitoring/modules/improved_mail_monitor.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация:
```bash
sudo systemctl daemon-reload
sudo systemctl enable server-monitor
sudo systemctl start server-monitor
```

Проверка состояния:
```bash
systemctl status server-monitor
```

## 📄 Лицензия
MIT License — подробности в файле [LICENSE](LICENSE).
