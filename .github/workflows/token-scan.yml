name: Token scan

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  telegram-token-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan for Telegram tokens
        run: ./scripts/check-telegram-tokens.sh
